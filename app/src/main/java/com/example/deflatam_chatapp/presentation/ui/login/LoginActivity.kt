package com.example.deflatam_chatapp.presentation.ui.login

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.deflatam_chatapp.R

/**
 * Actividad para la pantalla de inicio de sesión de la aplicación.
 */
class LoginActivity : AppCompatActivity() {
    /**
     * Se llama cuando la actividad se crea por primera vez.
     */
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_login)
        // Aquí se inicializarán los elementos de la UI de login.
    }
}